server:
  port: 8084
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/arodan10/microservicio_2023.git
          search-paths: config-data
          username: arodan10
          password: 17537
          default-label: main
  security:
    user:
      name: root
      password: s3cr3t
